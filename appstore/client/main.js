import React from 'react';
import ReactDOM from 'react-dom';
/*import NavBar from './components/navbar';
import AppDetail from './components/app_details';
import AppPage from './components/app_page';*/

import '/imports/routes';

/**
 * Some components use react-tap-event-plugin to listen for touch events because onClick is not fast enough 
 * This dependency is temporary and will eventually go away. Until then, be sure to inject this plugin at 
 * the start of your app.
 */
